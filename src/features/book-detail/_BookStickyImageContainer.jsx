import styled from "styled-components";

const BookStickyContainer = styled.div`
  position: sticky;
  position: sticky;
  inset-block-start: 0.5rem;
  block-size: fit-content;

  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  align-items: center;
`;

export default BookStickyContainer;
